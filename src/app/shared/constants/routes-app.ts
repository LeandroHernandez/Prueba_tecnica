export enum RoutesApp {
  root = '',
  auth = 'auth',
  logIn = 'log-in',
  sigIn = 'sign-in',
  register = 'sign-up',
  home = 'home',
  purchase = 'purchase',
  profile = 'profile',
  // account = 'account',
}
